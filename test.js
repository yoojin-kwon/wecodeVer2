function getEven() {
    const emptyArray = [];

    for(i=1; i<=50; i++){
        if(Number.isInteger(i/2) === true){
            const getArray = emptyArray.push(i);
        } 
    } return getArray;
}